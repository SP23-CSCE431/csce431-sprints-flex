<%= form_with(model: point) do |form| %>
  <% if point.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(point.errors.count, "error") %> prohibited this point from being saved:</h2>

      <ul>
        <% point.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :member_id, style: "display: block" %>
    <%= form.text_field :member_id %>
  </div>

  <div>
    <%= form.label :point_category_id, style: "display: block" %>
    <%= form.text_field :point_category_id %>
  </div>

  <div>
    <%= form.label :is_approved, style: "display: block" %>
    <%= form.check_box :is_approved %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
