<%= form_with(model: point_category) do |form| %>
  <% if point_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(point_category.errors.count, "error") %> prohibited this point_category from being saved:</h2>

      <ul>
        <% point_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :value, style: "display: block" %>
    <%= form.number_field :value %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
